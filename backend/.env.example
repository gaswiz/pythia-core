PORT=5000
FLASK_DEBUG=0
DATABASE_NAME=pythia
FRONTEND_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional: set to enable Mongo-backed persistence instead of local in-memory fallback.
MONGO_URI=
